!<arch>
LK/             1443303466  1000  1000  100664  8         `
content
two/            1442951913  1000  1000  100664  4         `
two
Linkcc/         1442978808  1000  1000  100444  729       `
/* Program to copy a file, using a specified buffer size */

#include <sys/types.h> 
#include <sys/stat.h>
#include <fcntl.h>
#include <stdlib.h> 
#include <unistd.h> 
#include <stdio.h> 
#include <errno.h>

int main (int argc, char** argv)
{
	int fd1,fd2;		/* file descriptors */
	char buff [4096];   /* overkill on buffer size*/
	int size;			/* real buffer size */
	int iosize;			/* actual amount read */

	if ((fd1=open(argv[1],0)) == -1) {
		perror("open1");
		exit(1);
		}

	if ((fd2=open(argv[2],O_WRONLY | O_CREAT |O_TRUNC,0644)) == -1) {
		perror("open2");
		exit(1);
		}

	size=atoi(argv[3]);

	while ((iosize=read(fd1,buff,size)) >0 )
		write(fd2,buff,iosize);

	if (iosize == -1) {
		perror("read");
		exit(1);
		}

}

four/           1442951922  1000  1000  100664  5         `
four

three/          1443303466  1000  1000  100664  8         `
content
one/            1442951905  1000  1000  100664  4         `
one
